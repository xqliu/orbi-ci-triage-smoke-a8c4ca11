{
  "name": "orbi-ci-triage-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Smoke test repository for the Orbi beta end-to-end delivery flow, with a real Node.js + vitest test suite.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "nanoid": "^6.0.1"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
